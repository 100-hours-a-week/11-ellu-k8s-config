apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: looper-prod
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    # 사용하는 네트워크 프로토콜
    - protocol: TCP
      port: 80
      targetPort: 8080